// Simple schema definitions for the cookbook database
// All schema operations are now handled directly in the main database file

export const DATABASE_NAME = 'cookbook.db';
export const DATABASE_VERSION = 1;

// Schema is now created directly in the database initialization
// No complex migration or setup functions needed